{
  "name": "munnai",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "build:daemon": "cargo build --release --manifest-path core/Cargo.toml --bin munnai-core",
    "export:daemon": "sh ./scripts/export-daemon.sh",
    "install:daemon": "sh ./scripts/install-daemon.sh",
    "dev": "pnpm -r dev"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
