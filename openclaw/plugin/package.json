{
  "name": "@muninn/openclaw-plugin-muninn",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && node --test test/**/*.test.mjs",
    "test:live": "pnpm build && pnpm --dir ../.. --filter @muninn/sidecar build && node --test test/live-e2e.mjs"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
