name: Common

on:
  pull_request:
    paths:
      - "common/**"
      - "package.json"
      - "pnpm-workspace.yaml"
      - ".github/workflows/common-ci.yml"
  push:
    branches:
      - main
    paths:
      - "common/**"
      - "package.json"
      - "pnpm-workspace.yaml"
      - ".github/workflows/common-ci.yml"

jobs:
  common:
    name: common-ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        env:
          MUNINN_SKIP_NATIVE_POSTINSTALL: "1"
        run: pnpm install --no-frozen-lockfile

      - name: Test @muninn/common
        run: pnpm --filter @muninn/common test
